[package]
name = "command-bus"
version = "0.1.0"
edition = "2024"

[dependencies]
core = { path = "../core" }

[target.'cfg(not (target_arch = "wasm32"))'.dependencies]
tokio =  { version = "1.50.0", features=["full"] }
